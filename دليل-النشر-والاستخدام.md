# 📘 دليل النشر والاستخدام — موقع سرايا الأندلس للأثاث الفندقي والضيافة

هذا الدليل مقسوم لجزأين:
1. **الجزء الأول**: كيف ترفع (تنشر) الموقع على الإنترنت **مجاناً** خطوة بخطوة.
2. **الجزء الثاني**: دليل مبسّط للأدمن — كيف تستخدم لوحة التحكم يومياً.

---

# الجزء الأول: نشر الموقع مجاناً 🚀

## لماذا Cloudflare Pages تحديداً؟

الموقع مبني بتقنية **Cloudflare Pages + قاعدة بيانات D1**، لذلك:

| المنصة | مجانية؟ | تشتغل مع الموقع؟ | السبب |
|--------|---------|------------------|-------|
| **Cloudflare Pages** ✅ | نعم بالكامل | ✅ نعم | مبني خصيصاً لها (D1 + Workers) |
| GitHub Pages | نعم | ❌ لا | ملفات ثابتة فقط — لا قاعدة بيانات ولا لوحة تحكم |
| Netlify | نعم | ❌ لا | لا تدعم قاعدة بيانات D1 |
| Vercel | نعم | ❌ لا | لا تدعم قاعدة بيانات D1 |
| Render (مجاني) | نعم | ❌ لا | يحتاج إعادة بناء كاملة للمشروع |

**الخلاصة: Cloudflare Pages هي الطريقة المجانية الوحيدة التي تشغّل الموقع كاملاً (الموقع + لوحة التحكم + قاعدة البيانات).**

الخطة المجانية من Cloudflare تشمل:
- استضافة غير محدودة للزوار (Bandwidth مجاني)
- 100,000 طلب يومياً للوحة التحكم وقاعدة البيانات (أكثر من كافٍ)
- قاعدة بيانات D1 مجانية (5 جيجا)
- شهادة أمان HTTPS تلقائية
- رابط مجاني: `اسم-مشروعك.pages.dev`

---

## الطريقة 1: النشر اليدوي من جهازك (الموصى بها)

### المتطلبات
- حساب مجاني على [cloudflare.com](https://dash.cloudflare.com/sign-up)
- تثبيت [Node.js](https://nodejs.org) (نسخة 18 أو أحدث) على جهازك

### الخطوة 1: إنشاء حساب Cloudflare
1. ادخل على https://dash.cloudflare.com/sign-up
2. سجّل بالإيميل وكلمة مرور — **لا يحتاج بطاقة بنكية**.
3. فعّل الإيميل من رسالة التأكيد.

### الخطوة 2: إنشاء مفتاح API Token
1. من لوحة Cloudflare: اضغط على أيقونة الحساب (أعلى يمين) → **My Profile**
2. من القائمة الجانبية: **API Tokens** → **Create Token**
3. اختر قالب **Edit Cloudflare Workers** → **Use template**
4. أضف الصلاحيات التالية (Add more):
   - `Account` → `Cloudflare Pages` → `Edit`
   - `Account` → `D1` → `Edit`
5. اضغط **Continue to summary** → **Create Token**
6. **انسخ التوكن واحفظه في مكان آمن** — لن يظهر مرة أخرى!

### الخطوة 3: تحميل المشروع وتجهيزه
افتح الطرفية (Terminal / CMD) ونفّذ:

```bash
# 1. تحميل المشروع من GitHub
git clone https://github.com/belalalibb/saray2and2.git
cd saray2and2

# 2. تثبيت الاعتماديات
npm install

# 3. ضبط التوكن (استبدل XXXX بالتوكن الذي نسخته)
# على ويندوز (CMD):
set CLOUDFLARE_API_TOKEN=XXXX
# على ويندوز (PowerShell):
$env:CLOUDFLARE_API_TOKEN="XXXX"
# على ماك/لينكس:
export CLOUDFLARE_API_TOKEN=XXXX
```

### الخطوة 4: إنشاء قاعدة البيانات الإنتاجية
```bash
npx wrangler d1 create webapp-production
```
سيظهر ناتج فيه `database_id` مثل:
```
database_id = "abc12345-6789-...."
```
**انسخ هذا الرقم** وافتح ملف `wrangler.jsonc` واستبدل قيمة `database_id` الموجودة (`local-placeholder`) بالرقم الجديد، ثم احفظ الملف.

### الخطوة 5: تجهيز جداول قاعدة البيانات والبيانات الأولية
```bash
# إنشاء الجداول على قاعدة البيانات الحقيقية
npx wrangler d1 migrations apply webapp-production --remote

# إدخال البيانات الأولية (حساب الأدمن + الإعدادات + المنتجات)
npx wrangler d1 execute webapp-production --remote --file=./seed.sql
```

### الخطوة 6: إنشاء المشروع والنشر
```bash
# بناء الموقع
npm run build

# إنشاء مشروع Pages (مرة واحدة فقط)
npx wrangler pages project create saraya-andalus --production-branch main

# النشر!
npx wrangler pages deploy dist --project-name saraya-andalus
```

🎉 **انتهيت!** سيظهر لك رابط مثل: `https://saraya-andalus.pages.dev`

### الخطوة 7: ربط قاعدة البيانات بالموقع المنشور
1. من لوحة Cloudflare: **Workers & Pages** → اختر مشروع `saraya-andalus`
2. **Settings** → **Bindings** → **Add** → **D1 database**
3. Variable name: اكتب `DB` (بالضبط هكذا)
4. اختر قاعدة `webapp-production` → **Save**
5. أعد النشر مرة أخيرة: `npx wrangler pages deploy dist --project-name saraya-andalus`

### التحديثات لاحقاً
أي تعديل مستقبلي، فقط:
```bash
npm run build
npx wrangler pages deploy dist --project-name saraya-andalus
```

---

## الطريقة 2: الربط التلقائي مع GitHub (تحديث تلقائي عند كل تعديل)

1. من لوحة Cloudflare: **Workers & Pages** → **Create** → **Pages** → **Connect to Git**
2. اربط حساب GitHub واختر مستودع `belalalibb/saray2and2`
3. إعدادات البناء (Build settings):
   - **Framework preset**: None
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
4. اضغط **Save and Deploy**
5. بعد أول نشر: اذهب لـ **Settings → Bindings** وأضف قاعدة D1 باسم `DB` (كما في الخطوة 7 أعلاه)
6. طبّق الـ migrations والـ seed من جهازك (الخطوات 4 و 5 من الطريقة الأولى)

**الميزة**: أي `git push` إلى GitHub = نشر تلقائي للموقع خلال دقائق.

---

## ربط دومين خاص (اختياري — مجاني أيضاً)
إذا عندك دومين مثل `saraya-andalus.com`:
1. من مشروع Pages: **Custom domains** → **Set up a custom domain**
2. اكتب الدومين واتبع التعليمات (تغيير DNS records عند مزوّد الدومين)
3. شهادة HTTPS تصدر تلقائياً ومجاناً.

---

## استكشاف الأخطاء الشائعة

| المشكلة | الحل |
|---------|------|
| `Authentication error` | تأكد أن التوكن مضبوط في المتغير `CLOUDFLARE_API_TOKEN` وأن صلاحياته تشمل Pages + D1 |
| الموقع يفتح لكن "خطأ في الخادم" | قاعدة البيانات غير مربوطة — راجع الخطوة 7 (Binding باسم `DB`) |
| لوحة التحكم ترفض الدخول | لم يُنفَّذ seed.sql على القاعدة الإنتاجية — راجع الخطوة 5 |
| الصور لا تظهر | تأكد أن مجلد `public/static/images` موجود قبل `npm run build` |
| `wrangler: command not found` | استخدم `npx wrangler` بدلاً من `wrangler` |

---
---

# الجزء الثاني: دليل الأدمن المبسّط 👨‍💼

## الدخول للوحة التحكم

1. افتح: `https://رابط-موقعك/admin`
2. بيانات الدخول الافتراضية:
   - **البريد**: `admin@saraya-andalus.com`
   - **كلمة المرور**: `SarayaAdmin@2026`

> ⚠️ **مهم جداً**: بعد أول دخول، غيّر كلمة المرور فوراً من قسم **المستخدمون** → تعديل حسابك → كلمة مرور جديدة.

الجلسة تبقى فعالة 12 ساعة ثم يُطلب الدخول مجدداً.

---

## أقسام لوحة التحكم (10 أقسام)

### 1️⃣ نظرة عامة (Dashboard)
أول شاشة بعد الدخول. تعرض:
- عدد المنتجات والفئات والخدمات والمشاريع
- عدد الطلبات الجديدة (عروض الأسعار + رسائل التواصل)
- آخر الطلبات الواردة

### 2️⃣ الطلبات (CRM) — أهم قسم يومياً 📞
هنا تصل كل طلبات العملاء من الموقع (طلب عرض سعر / رسالة تواصل):
- **كل طلب له حالة**: جديد → تم التواصل → عرض سعر مُرسل → صفقة ناجحة / مرفوض
- اضغط على الطلب لعرض التفاصيل الكاملة (الاسم، الهاتف، الرسالة)
- **زر واتساب** بجانب كل طلب للتواصل المباشر مع العميل
- يمكنك إضافة **ملاحظات** على كل طلب (مثلاً: "اتصلت به يوم الأحد — ينتظر العرض")
- **نصيحة**: راجع هذا القسم يومياً وحدّث حالة كل طلب

### 3️⃣ المنتجات
- **إضافة منتج**: زر "إضافة منتج" → املأ الاسم، الوصف، الفئة، السعر (اختياري)، الصور
- **الصور**: اختر من مكتبة الصور الجاهزة أو ضع رابط صورة
- **منتج مميز**: فعّل خيار "مميز" ليظهر في الصفحة الرئيسية
- **مسودة/منشور**: المسودة لا تظهر للزوار — انشر فقط عندما يكون المنتج جاهزاً

### 4️⃣ الفئات
تصنيفات المنتجات (غرف نوم فندقية، صالونات، مطاعم...). يمكنك التعديل والإضافة والترتيب.

### 5️⃣ الخدمات
خدمات الشركة المعروضة (تصنيع مخصص، تجهيز متكامل، تصميم...). عدّل النصوص والأيقونات والصور.

### 6️⃣ المشاريع
سابقة الأعمال (المشاريع المنفذة). أضف اسم المشروع، العميل، الوصف، وصور من التنفيذ — هذا القسم يبني الثقة عند الزوار.

### 7️⃣ الصفحة الرئيسية
تحكّم في أقسام الصفحة الرئيسية للموقع:
- عدّل عنوان ونص كل قسم (الواجهة، عن الشركة، دعوة التواصل...)
- فعّل أو عطّل أي قسم
- عدّل نقاط "لماذا نحن" (المميزات التنافسية)

### 8️⃣ الإعدادات
البيانات الأساسية للشركة:
- اسم الشركة، الهاتف، **رقم الواتساب** (مهم — كل أزرار واتساب في الموقع تستخدمه)
- العنوان ومواعيد العمل
- نصوص رسائل واتساب الجاهزة
- روابط فيسبوك وإنستجرام
- عناوين SEO لمحركات البحث

### 9️⃣ المستخدمون (للمدير العام فقط)
إضافة موظفين للوحة التحكم بصلاحيات مختلفة:

| الدور | ماذا يستطيع؟ |
|------|--------------|
| **مدير عام** | كل شيء + إدارة المستخدمين + سجل النشاط |
| **مدير محتوى** | المنتجات والفئات والخدمات والمشاريع والصفحة الرئيسية والإعدادات |
| **مبيعات** | الطلبات (CRM) فقط |
| **محرر** | تعديل المحتوى بدون حذف |

- **نصيحة أمان**: أعطِ كل موظف أقل صلاحية يحتاجها. موظف المبيعات يكفيه دور "مبيعات".

### 🔟 سجل النشاط (للمدير العام فقط)
سجل كامل بكل ما حدث في اللوحة: من عدّل ماذا ومتى. مفيد للمراجعة والمحاسبة.

---

## المهام اليومية المقترحة للأدمن ✅

1. **صباحاً**: افتح "الطلبات" → تواصل مع الطلبات الجديدة عبر واتساب → حدّث حالتها
2. **عند إضافة بضاعة جديدة**: المنتجات → إضافة منتج → صور واضحة + وصف جيد → نشر
3. **عند إنجاز مشروع**: المشاريع → أضفه بالصور (يقوّي سمعة الشركة)
4. **شهرياً**: راجع الإعدادات (الأرقام والروابط صحيحة؟) وسجل النشاط

## نصائح أمان 🔒
- غيّر كلمة المرور الافتراضية **فوراً** بعد أول دخول
- كلمة المرور: 8 أحرف على الأقل، امزج أرقاماً وحروفاً ورموزاً
- لا تشارك حسابك — أنشئ حساباً مستقلاً لكل موظف
- عطّل حساب أي موظف يترك العمل (المستخدمون → تعديل → إلغاء "نشط")

## مشاكل شائعة وحلولها
| المشكلة | الحل |
|---------|------|
| نسيت كلمة المرور | مدير عام آخر يغيّرها لك من "المستخدمون"، أو أعد تنفيذ seed.sql |
| "انتهت الجلسة" | طبيعي بعد 12 ساعة — سجّل الدخول مجدداً |
| تعديل لا يظهر في الموقع | حدّث الصفحة (Ctrl+F5)؛ تأكد أن الحالة "منشور" وليست "مسودة" |
| زر واتساب يفتح رقماً خاطئاً | الإعدادات → صحّح حقل "رقم واتساب" → حفظ |
